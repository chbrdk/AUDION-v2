# msqdx-glass-proto

Shared Pydantic models for persona profiles, chat events, and document ingestion results. These schemas keep the FastAPI backend, Celery workers, and frontend type generation aligned.

