services:
  postgres:
    image: postgres:17
    container_name: persona-postgres
    environment:
      POSTGRES_DB: persona
      POSTGRES_USER: persona
      POSTGRES_PASSWORD: persona
    ports:
      - "55432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: persona-redis
    ports:
      - "6380:6379"

  qdrant:
    image: qdrant/qdrant:v1.11.3
    container_name: persona-qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant-data:/qdrant/storage

  neo4j:
    image: neo4j:5.22.0
    container_name: persona-neo4j
    environment:
      NEO4J_AUTH: neo4j/neo4j_password
    ports:
      - "7687:7687"
      - "7474:7474"
    volumes:
      - neo4j-data:/data

  tempo:
    image: grafana/tempo:2.6.0
    container_name: persona-tempo
    command: [ "-config.file=/tempo.yaml" ]
    volumes:
      - ./tempo.yml:/tempo.yaml
      - tempo-data:/tmp/tempo
    ports:
      - "4318:4318"

  indexing-api:
    build:
      context: ..
      dockerfile: apps/indexing-api/Dockerfile
    container_name: persona-indexing-api
    environment:
      APP_ENV: development
      API_HOST: 0.0.0.0
      API_PORT: 8000
      DATA_DIR: /app/data/uploads
      DATABASE_URL: postgresql+psycopg://persona:persona@postgres:5432/persona
      REDIS_URL: redis://redis:6379/0
      QDRANT_URL: http://qdrant:6333
      OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo:4318
      LOGFIRE_TOKEN: ${LOGFIRE_TOKEN:-}
    volumes:
      - ../data:/app/data
      - huggingface-cache:/root/.cache/huggingface
    depends_on:
      - postgres
      - redis
      - qdrant
      - tempo
    expose:
      - "8000"

  indexing-worker:
    build:
      context: ..
      dockerfile: apps/indexing-api/Dockerfile
    container_name: persona-indexing-worker
    command: celery -A app.workers.process worker -Q ingestion -l info --pool=threads --concurrency=2
    environment:
      APP_ENV: development
      DATA_DIR: /app/data/uploads
      DATABASE_URL: postgresql+psycopg://persona:persona@postgres:5432/persona
      REDIS_URL: redis://redis:6379/0
      QDRANT_URL: http://qdrant:6333
      OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo:4318
      LOGFIRE_TOKEN: ${LOGFIRE_TOKEN:-}
    volumes:
      - ../data:/app/data
      - huggingface-cache:/root/.cache/huggingface
    depends_on:
      - indexing-api
      - postgres
      - redis
      - qdrant

  chat-api:
    build:
      context: ..
      dockerfile: apps/chat-api/Dockerfile
    container_name: persona-chat-api
    env_file:
      - ../.env
    environment:
      APP_ENV: development
      API_HOST: 0.0.0.0
      API_PORT: 8001
      DATABASE_URL: postgresql+psycopg://persona:persona@postgres:5432/persona
      QDRANT_URL: http://qdrant:6333
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: neo4j
      NEO4J_PASSWORD: neo4j_password
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      INDEXING_API_URL: http://indexing-api:8000
      OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo:4318
      LOGFIRE_TOKEN: ${LOGFIRE_TOKEN:-}
    volumes:
      - huggingface-cache:/root/.cache/huggingface
    depends_on:
      - postgres
      - qdrant
      - neo4j
      - tempo
    expose:
      - "8001"

  web:
    build:
      context: ..
      dockerfile: apps/web/Dockerfile
    container_name: persona-web
    env_file:
      - ../.env
    environment:
      NEXT_PUBLIC_INDEXING_API_URL: https://192.168.50.101/api/indexing
      NEXT_PUBLIC_CHAT_API_URL: https://192.168.50.101/api/chat
      NEXT_PUBLIC_WS_BASE_URL: wss://192.168.50.101/api/chat
      NEXT_BACKEND_INTERNAL_URL: http://indexing-api:8000
      NEXT_CHAT_API_INTERNAL_URL: http://chat-api:8001
    depends_on:
      - indexing-api
      - chat-api
    expose:
      - "3000"

  nginx:
    image: nginx:alpine
    container_name: persona-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - web
      - chat-api
      - indexing-api

volumes:
  postgres-data:
  qdrant-data:
  neo4j-data:
  tempo-data:
  huggingface-cache:
