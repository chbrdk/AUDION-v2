"""Central path suffixes for outbound HTTP calls (no host — host comes from settings)."""

# PLEXON control plane (Next.js app): register an AUDION-native project and mirror to CHECKION.
PLEXON_PROVISIONING_AUDION_PROJECT_ORIGIN_PATH = "/api/platform/provisioning/audion-project-origin"
