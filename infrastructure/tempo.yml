server:
  http_listen_port: 4318
  log_level: info

distributor:
  receivers:
    otlp:
      protocols:
        http:

ingester:
  max_block_duration: 5m

compactor:
  compaction:
    compacted_block_retention: 48h

storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo

